# refresh2025

# run
 
```bash
flutter run
```

# setup
* get flutter sdk
* extract tar-ball 
* update `~/.profile`
* run `flutter doctor`
* focus is web so we just need chrome 
* create project `flutter create refresh2025`
